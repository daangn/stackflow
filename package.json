{
  "name": "@karrotframe/monorepo",
  "license": "Apache-2.0",
  "scripts": {
    "build": "yarn workspaces foreach --topological-dev run build",
    "prettify": "prettier --write .",
    "test:navigator": "cd packages/navigator && yarn test",
    "install:navigator": "cd packages/navigator && yarn install --immutable --immutable-cache"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "prettier": "^2.3.2",
    "typescript": "^4.3.5"
  },
  "packageManager": "yarn@3.1.1"
}
