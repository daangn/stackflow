---
"@stackflow/config": major
---

`@stackflow/plugin-map-initial-activity` package removed. Use `initialActivity` in `defineConfig()` instead; move the initial activity mapping into `defineConfig({ initialActivity: () => "ActivityName" })`.
