# `@stackflow/plugin-basic-ui`

전역 스택 상태를 사용하여 활동 내에서 UI를 렌더링합니다. 기본적으로 `cupertino` 및 `android` 테마를 제공합니다.

## 설치

```bash npm2yarn copy
npm install @stackflow/plugin-basic-ui
```

## 사용법

```tsx showLineNumbers filename="stackflow.ts" copy
import { stackflow } from "@stackflow/react";
import { basicUIPlugin } from "@stackflow/plugin-basic-ui";

const { Stack, useFlow } = stackflow({
  // ...
  plugins: [
    // ...
    basicUIPlugin({
      theme: "cupertino",
    }),
  ],
});
```

```tsx filename="AppScreen" copy
import { AppScreen } from "@stackflow/plugin-basic-ui";

const Something = () => {
  return (
    <AppScreen appBar={{ title: "Home" }}>
      <div>Hello, World</div>
    </AppScreen>
  );
};
```

```tsx filename="Modal" copy
import { Modal } from "@stackflow/plugin-basic-ui";

const Something = () => {
  return (
    <Modal>
      <div>Hello, World</div>
    </Modal>
  );
};
```

```tsx filename="BottomSheet" copy
import { BottomSheet } from "@stackflow/plugin-basic-ui";

const Something = () => {
  return (
    <BottomSheet>
      <div>Hello, World</div>
    </BottomSheet>
  );
};
```
