# 사용할 수 없는 탐색 Snapshot에서 기본 복구

저장된 탐색 Snapshot이 없으면 새 Stack을 생성하고, Snapshot이 손상됐거나 현재 앱과 호환되지 않으면 오류를 관찰 가능하게 알린 뒤 해당 Snapshot을 포기하고 새 Stack으로 복구한다. 오래된 보존 데이터가 앱 시작을 계속 막지 않게 하는 것을 기본값으로 삼되, 탐색 연속성을 필수 조건으로 취급하는 소비자는 오류 전파를 선택할 수 있다.
