import { useActivity } from "@stackflow/react";
import React from "react";

const CupertinoAppScreen: React.FC = () => {
  const activity = useActivity();
  return <div>cupertino</div>;
};

export default CupertinoAppScreen;
