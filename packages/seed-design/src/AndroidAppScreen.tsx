import { useActivity } from "@stackflow/react";
import React from "react";

const AndroidAppScreen: React.FC = () => {
  const activity = useActivity();

  return <div>android</div>;
};

export default AndroidAppScreen;
